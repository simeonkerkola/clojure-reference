(ns clojure-tutorial.core
  (:gen-class))

(def randVar 10)

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (def aLong 15)
  (println (nil? aLong))
  )

  ;; 6.10
  ;; run $ lein auto run
